version: 1
common:
    appLogDestination: console
    enableAppHealthCheck: false
      # appHealthCheckPath: /healthz
      # appHealthProbeInterval: 5
      # appHealthProbeTimeout: 500
      # appHealthThreshold: 3
apps:
    - appID: cypher-generator
      appPort: 0
      appDirPath: .
      command:
      - npm
      - run
      - start
      appProtocol: http
      logLevel: info
      enableAppHealthCheck: false
        # appHealthCheckPath: /healthz
        # appHealthProbeInterval: 5
        # appHealthProbeTimeout: 500
        # appHealthThreshold: 3
      appLogDestination: console
 
